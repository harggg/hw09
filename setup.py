from setuptools import setup, find_packages

setup(
    name='booklover',
    version='0.1',
    packages=find_packages(),
    author='harggg',
    description='package hw09',
    install_requires=[],
)
